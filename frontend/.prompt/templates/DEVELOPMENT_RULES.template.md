# 프론트엔드 개발 규칙 (Frontend Development Rules)

## 기능 구현 순서

### 1. 구현 순서
기능 구현 시 다음 순서를 따른다:
1. **[첫 번째 단계]** (예: API Service 함수 작성)
2. **[두 번째 단계]** (예: 컴포넌트 구현)
3. **[세 번째 단계]** (예: 상태 관리 연결)
4. **[네 번째 단계]** (예: 브라우저 테스트)

### 2. 테스트 및 검증
[단계] 구현 완료 후 다음 절차를 수행한다:
1. **[테스트 방법 1]**
2. **[테스트 방법 2]**
3. **[테스트 방법 3]**
4. **[테스트 방법 4]**

## 코드 작성 규칙

### 1. 프로젝트 구조
**[구조 방식]**

```
src/
├── [디렉토리1]/
│   ├── [하위 디렉토리1]/
│   └── [하위 디렉토리2]/
├── [디렉토리2]/
└── [디렉토리3]/
```

### 2. 네이밍 컨벤션

#### 파일명
- **[타입1]**: `[패턴]` (예: PascalCase.jsx)
- **[타입2]**: `[패턴]` (예: camelCase.js)

#### 변수/함수명
- **[타입1]**: `[패턴]` (예: PascalCase)
- **[타입2]**: `[패턴]` (예: camelCase)
- **[타입3]**: `[패턴]` (예: UPPER_SNAKE_CASE)

#### CSS 클래스명
- **[패턴]** (예: kebab-case)

### 3. 컴포넌트 작성 규칙

#### [컴포넌트 타입] 사용
```[언어]
// ✅ 올바른 예시
[올바른 코드]

// ❌ 잘못된 예시
[잘못된 코드]
```

#### Props 처리
```[언어]
[Props 코드 예시]
```

### 4. Hooks 사용 규칙

#### Hooks 호출 순서
- [규칙 1]
- [규칙 2]

```[언어]
[Hooks 코드 예시]
```

### 5. API 호출 규칙

#### Service 레이어 사용
```[언어]
// ❌ 잘못된 예시
[잘못된 코드]

// ✅ 올바른 예시
[올바른 코드]
```

#### 에러 처리
```[언어]
[에러 처리 코드 예시]
```

#### 로딩 상태 관리
```[언어]
[로딩 상태 코드 예시]
```

### 6. 상태 관리 규칙

#### 로컬 상태 vs 전역 상태
- **로컬 상태**: [사용 시점]
- **전역 상태**: [사용 시점]

#### Context 사용
```[언어]
[Context 코드 예시]
```

### 7. 성능 최적화

#### [최적화 방법 1]
```[언어]
[코드 예시]
```

#### [최적화 방법 2]
```[언어]
[코드 예시]
```

### 8. 스타일링 규칙

#### [스타일링 방법]
```[언어]
[스타일 코드 예시]
```

#### 조건부 클래스
```[언어]
[조건부 클래스 코드 예시]
```

### 9. 폼 처리

#### [폼 처리 방법]
```[언어]
[폼 코드 예시]
```

#### 입력 검증
```[언어]
[검증 코드 예시]
```

### 10. 에러 처리

#### 에러 바운더리
```[언어]
[에러 바운더리 코드 예시]
```

#### API 에러 처리
```[언어]
[API 에러 처리 코드 예시]
```

### 11. 라우팅 규칙

#### Protected Route 구현
```[언어]
[Protected Route 코드 예시]
```

### 12. 코드 품질 원칙

#### [원칙 1]
- [세부 내용]

#### [원칙 2]
- [세부 내용]

### 13. 접근성 (Accessibility)

#### 시맨틱 HTML
```[언어]
[시맨틱 HTML 예시]
```

#### ARIA 속성
```[언어]
[ARIA 코드 예시]
```

### 14. 유틸리티 함수

#### [유틸리티 카테고리]
```[언어]
[유틸리티 함수 예시]
```

### 15. Git 커밋 규칙

#### 커밋 메시지 형식
```
<type>: <subject>

<body>
```

#### Type 종류
- `feat`: 새로운 기능
- `fix`: 버그 수정
- `style`: UI/스타일 변경
- `refactor`: 리팩토링
- `docs`: 문서 수정
- `chore`: 빌드 설정

#### 예시
```
[커밋 메시지 예시]
```

### 16. 주석 작성

#### [주석 규칙]
```[언어]
[주석 코드 예시]
```

### 17. 보안 규칙

#### [보안 항목 1]
```[언어]
[보안 코드 예시]
```

#### [보안 항목 2]
- [규칙 설명]

## 작업 체크리스트

각 기능 구현 시 다음 체크리스트를 확인한다:

- [ ] [단계 1] 완료
- [ ] [단계 2] 완료
- [ ] [단계 3] 완료
- [ ] [테스트] 성공
- [ ] 콘솔 에러/경고 없음
- [ ] Git 커밋 완료

## 주의사항

- 이 규칙은 프로젝트 진행 중 수정되거나 추가될 수 있음
- 규칙 변경 시 이 문서를 업데이트할 것
- 모든 팀원은 이 규칙을 준수할 것

---

## 변경 이력

| 날짜 | 변경 내용 | 작성자 |
|------|----------|--------|
| YYYY-MM-DD | 초기 작성 | - |
